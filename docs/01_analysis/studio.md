# Studio Framework 模块需求分析

**模块**: 工作室框架 (Studio Framework)
**分析日期**: 2026-01-11
**状态**: 🔄 分析中

---

## 1. 业务背景

用户进入项目后，需要一个集成的工作环境来管理来源、进行 AI 对话、生成各类输出。工作室采用三栏布局，将核心功能模块化展示，支持面板折叠以适应不同工作场景。

**用户痛点**:
- 需要同时查看来源和进行对话
- 需要快速访问输出工具
- 不同任务需要不同的界面布局
- 小屏幕需要隐藏部分面板

**参考设计**: NotebookLM 工作室界面

---

## 2. 业务主角

| 主角 | 描述 |
|:---|:---|
| 用户 (User) | 在工作室中进行研究、对话、生成输出 |
| 系统 (System) | 管理面板状态、持久化布局偏好 |

---

## 3. 领域对象

| 对象 | 类型 | 职责 |
|:---|:---|:---|
| Studio | Entity (聚合根) | 工作室容器，管理三栏布局状态 |
| Panel | Value Object | 面板配置（标识、宽度、折叠状态） |
| PanelType | Enum | 面板类型枚举 |
| LayoutPreference | Value Object | 用户布局偏好设置 |

### 3.1 Studio 实体结构

```typescript
interface Studio {
  projectId: string;           // 关联项目 ID
  panels: PanelState[];        // 面板状态列表
  activePanel: PanelType;      // 当前激活面板（用于移动端）
}

interface PanelState {
  type: PanelType;             // 面板类型
  isCollapsed: boolean;        // 是否折叠
  width: number;               // 宽度（像素）
}

enum PanelType {
  Sources = 'sources',         // 来源面板
  Chat = 'chat',               // 对话面板
  Workspace = 'workspace'      // 工作区面板
}

interface LayoutPreference {
  sourcesWidth: number;        // 来源面板宽度，默认 300px
  workspaceWidth: number;      // 工作区宽度，默认 340px
  sourcesCollapsed: boolean;   // 来源面板默认折叠状态
  workspaceCollapsed: boolean; // 工作区默认折叠状态
}
```

### 3.2 面板默认配置

| 面板 | 默认宽度 | 最小宽度 | 最大宽度 | 可折叠 |
|:---|:---|:---|:---|:---|
| Sources (来源) | 300px | 240px | 400px | Yes |
| Chat (对话) | flex-1 | 400px | - | No |
| Workspace (工作区) | 340px | 280px | 480px | Yes |

---

## 4. 用例清单

| UC-ID | 用例名称 | 主角 | 优先级 | 关联需求 |
|:---|:---|:---|:---|:---|
| UC-S01 | 进入项目工作室 | 用户 | P0 | REQ-F-040 |
| UC-S02 | 查看三栏布局 | 用户 | P0 | REQ-F-040 |
| UC-S03 | 折叠面板 | 用户 | P2 | REQ-F-041 |
| UC-S04 | 展开面板 | 用户 | P2 | REQ-F-041 |
| UC-S05 | 返回首页 | 用户 | P0 | REQ-F-040 |

---

## 5. 用例详情

### UC-S01: 进入项目工作室

**主角**: 用户
**前置条件**: 项目已存在
**触发条件**: 用户在首页点击项目卡片

**基本流程**:
1. 用户在首页点击某个项目卡片
2. 系统导航到工作室路由 `/project/:id`
3. 系统加载项目数据
4. 系统渲染三栏布局
5. 系统恢复用户的布局偏好（面板折叠状态）
6. 系统显示项目标题和操作栏

**异常流程**:
- E1: 项目不存在 → 显示错误提示，返回首页
- E2: 项目数据加载失败 → 显示重试选项

**验收条件**:
```
AC-1: 项目路由跳转
Given: 用户在首页
When: 用户点击项目卡片
Then: URL 变为 /project/{projectId}，显示工作室界面

AC-2: 项目标题显示
Given: 工作室界面加载完成
When: 用户查看顶部导航
Then: 显示项目图标和项目名称
```

---

### UC-S02: 查看三栏布局

**主角**: 用户
**前置条件**: 已进入项目工作室
**触发条件**: 工作室页面加载

**基本流程**:
1. 系统渲染三栏布局容器
2. 左侧显示来源面板（Sources）
3. 中间显示对话面板（Chat）
4. 右侧显示工作区面板（Workspace）
5. 各面板显示标题栏和内容区

**验收条件**:
```
AC-1: 三栏布局结构
Given: 用户进入工作室
When: 页面加载完成
Then: 显示三栏布局（来源-对话-工作区）

AC-2: 来源面板结构
Given: 工作室界面可见
When: 查看左侧面板
Then: 显示"来源"标题、折叠按钮、添加来源按钮、来源列表

AC-3: 对话面板结构
Given: 工作室界面可见
When: 查看中间面板
Then: 显示"对话"标题、对话消息区、输入框

AC-4: 工作区面板结构
Given: 工作室界面可见
When: 查看右侧面板
Then: 显示"工作区"标题、折叠按钮、输出工具网格
```

---

### UC-S03: 折叠面板

**主角**: 用户
**前置条件**: 面板处于展开状态
**触发条件**: 用户点击面板折叠按钮

**基本流程**:
1. 用户点击面板标题栏的折叠按钮
2. 系统动画收起面板
3. 系统显示折叠条（包含展开按钮和面板标识）
4. 系统保存折叠状态到本地存储
5. 中间对话面板自动扩展占用释放的空间

**验收条件**:
```
AC-1: 来源面板折叠
Given: 来源面板处于展开状态
When: 用户点击来源面板的折叠按钮
Then: 来源面板收起为窄条，对话面板扩展

AC-2: 工作区面板折叠
Given: 工作区面板处于展开状态
When: 用户点击工作区面板的折叠按钮
Then: 工作区面板收起为窄条，对话面板扩展

AC-3: 折叠状态持久化
Given: 用户折叠了某个面板
When: 用户刷新页面或重新进入项目
Then: 面板保持折叠状态
```

---

### UC-S04: 展开面板

**主角**: 用户
**前置条件**: 面板处于折叠状态
**触发条件**: 用户点击折叠条的展开按钮

**基本流程**:
1. 用户点击折叠条上的展开按钮
2. 系统动画展开面板
3. 系统恢复面板原始宽度
4. 系统保存展开状态到本地存储

**验收条件**:
```
AC-1: 面板展开
Given: 面板处于折叠状态
When: 用户点击展开按钮
Then: 面板动画展开到原始宽度

AC-2: 展开状态持久化
Given: 用户展开了某个面板
When: 用户刷新页面
Then: 面板保持展开状态
```

---

### UC-S05: 返回首页

**主角**: 用户
**前置条件**: 用户在工作室界面
**触发条件**: 用户点击返回按钮或 Logo

**基本流程**:
1. 用户点击顶部导航的 Logo 或返回按钮
2. 系统导航到首页 `/`
3. 系统记录项目访问时间（最近访问）

**验收条件**:
```
AC-1: 返回首页
Given: 用户在工作室界面
When: 用户点击 Logo 或返回按钮
Then: 导航到首页，显示项目列表
```

---

## 6. UI 组件结构

```
StudioPage
├── StudioHeader                    # 顶部导航栏
│   ├── BackButton                  # 返回首页
│   ├── ProjectIcon                 # 项目图标
│   ├── ProjectTitle                # 项目名称
│   └── HeaderActions               # 操作按钮（分享、设置等）
│
└── StudioLayout                    # 三栏布局容器
    ├── SourcesPanel                # 来源面板 (左)
    │   ├── PanelHeader             # "来源" + 折叠按钮
    │   ├── AddSourceButton         # 添加来源按钮
    │   ├── SourceSearch            # 搜索来源
    │   └── SourceList              # 来源列表
    │
    ├── ChatPanel                   # 对话面板 (中)
    │   ├── PanelHeader             # "对话" + 操作按钮
    │   ├── MessageList             # 消息列表
    │   └── ChatInput               # 输入区域
    │
    └── WorkspacePanel              # 工作区面板 (右)
        ├── PanelHeader             # "工作区" + 折叠按钮
        ├── OutputTools             # 输出工具网格
        └── SavedOutputs            # 已保存的输出
```

---

## 7. 面板交互规范

### 7.1 折叠按钮图标

| 面板 | 展开时图标 | 折叠时图标 |
|:---|:---|:---|
| Sources | `left_panel_close` | `left_panel_open` |
| Workspace | `right_panel_close` | `right_panel_open` |

### 7.2 折叠动画

- 动画时长: 200ms
- 缓动函数: ease-in-out
- 折叠条宽度: 48px

### 7.3 响应式行为

| 屏幕宽度 | 布局调整 |
|:---|:---|
| >= 1280px | 三栏全部显示 |
| 1024-1279px | 来源面板默认折叠 |
| 768-1023px | 来源和工作区面板默认折叠 |
| < 768px | 单面板切换模式（底部 Tab 导航） |

---

## 8. 数据流图

```
┌─────────────┐     ┌─────────────┐     ┌─────────────┐
│   首页      │────>│ 工作室路由   │────>│  三栏布局   │
│ (HomePage)  │     │ /project/:id │     │ (Studio)   │
└─────────────┘     └─────────────┘     └─────────────┘
                           │
                           ▼
                    ┌─────────────┐
                    │  项目数据    │
                    │  加载器     │
                    └─────────────┘
                           │
              ┌────────────┼────────────┐
              ▼            ▼            ▼
       ┌──────────┐ ┌──────────┐ ┌──────────┐
       │ Sources  │ │   Chat   │ │Workspace │
       │  Panel   │ │  Panel   │ │  Panel   │
       └──────────┘ └──────────┘ └──────────┘
              │            │            │
              └────────────┼────────────┘
                           ▼
                    ┌─────────────┐
                    │ LocalStorage│
                    │ 布局偏好    │
                    └─────────────┘
```

---

## 9. 状态管理

### 9.1 Studio 状态

```typescript
interface StudioState {
  // 项目数据
  project: Project | null;
  isLoading: boolean;
  error: string | null;

  // 面板状态
  sourcesCollapsed: boolean;
  workspaceCollapsed: boolean;

  // 动作
  toggleSourcesPanel: () => void;
  toggleWorkspacePanel: () => void;
}
```

### 9.2 持久化策略

| 状态 | 存储位置 | Key |
|:---|:---|:---|
| 面板折叠状态 | LocalStorage | `studio:panel:sources:collapsed` |
| 面板折叠状态 | LocalStorage | `studio:panel:workspace:collapsed` |
| 面板宽度 | LocalStorage | `studio:panel:sources:width` |
| 面板宽度 | LocalStorage | `studio:panel:workspace:width` |

---

## 10. 非功能约束

| 约束 | 描述 |
|:---|:---|
| 性能 | 工作室页面首次加载 < 500ms |
| 动画 | 面板折叠/展开动画流畅，无卡顿 |
| 响应式 | 支持 768px 以上屏幕宽度 |
| 可访问性 | 面板可通过键盘导航 |

---

## 11. 依赖关系

| 依赖模块 | 说明 |
|:---|:---|
| Project 模块 | 提供项目数据加载 |
| 路由系统 | 提供项目路由 `/project/:id` |

---

**文档版本**: v1.0
**作者**: Claude 需求分析师
**下一步**: 更新 RTM → 提交审计 → 技术设计
